export enum VueVersion {
  v2 = "v2",
  v3 = "v3"
}

/**
 * `BuildSubdirectory` is the type for an optional argument to the CodeGenManager constructor
 */
export type BuildSubdirectory = VueVersion | "";